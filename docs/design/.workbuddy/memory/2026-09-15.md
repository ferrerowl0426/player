# 2026-09-15

## 学生管理页 · 新增「学生标记」（教导主任专属）
- 改动文件：`docs/design/guitar-home/student-manage.html`（设计稿）、`docs/design/guitar-home/design-spec.md`（§2.5 新增标记紫 token）
- 需求：给学生做标记 → 被标记学生显示特殊小圆点 → 可一键取消标记。用户明确「不要乱加东西」，未做筛选/排序/备注等扩展
- 配色：用户原本要红色，后放宽为紫/蓝皆可；因 `#b4452e` 已被「停用/删除」占用，定 **标记紫 `#7b5ea7`**（白底对比度约 5.2:1，过 WCAG AA）
- 交互规格：
  - 列表行内圆点 `.mk`：未标记=咖啡色空心，hover 行时浮现；已标记=紫色实心常驻；点击即切换（toast 反馈）
  - 中列学生头部按钮 `.mk-tag`：文案在「标记学生 / 取消标记」间切换，已标记为紫色实底
  - 仅 `data-view="dean"` 渲染，沿用 `.dean-only` 口径；老师视角完全不渲染
  - 圆点用 `<span role=button tabindex=0>`（避免 button 嵌套 button），keydown 处理 Enter/Space 并阻止冒泡，重绘后找回焦点
- 假数据：林小满、周予安 预置 `mk:true`
- 未改动 PRD.md（业务侧如需补一条权限说明，应在 §5.6 加一行）

## 追加：一键清除全部标记
- 入口：左列「选择学生」区头右侧 `.mk-clear`（design-spec §7.2 的教导主任专属操作位），带已标记人数，**无标记时不渲染**
- 作用范围：**全部学生**，不受班级/搜索筛选影响；二次确认弹层（复用 `.modal` 壳，确认键用紫底 `.mbtn.mark`，不用危险红——不是删除操作）
- 文件：`student-manage.html`；design-spec §2.5 同步补一条清除入口说明
- 微调（用户截图反馈）：`.mk-clear` 去掉 `margin-left:auto`、字号 10→9.5px、padding 收紧，改为紧跟「共 N 人」并排的小徽标，不再换行

## 追加：停用学生不参与标记
- `normalizeMarks()`：停用学生（`dis:true`）一律 `mk=false`，在 `renderStuList()` 开头执行 → 停用即失去标记
- 三重拦截：列表行不渲染圆点（`isDean() && !s.dis`）、`toggleMark()` 早退、头部 `.mk-tag` 对停用学生隐藏
- 清除计数 `markCount()` 自然排除停用学生
- design-spec §2.5 同步补一条规则
