# PRD — 能学慧吉他教室 · 内部视频课程平台

> 版本：**v1.0（开发版）** · 2026-09-11  
> 定位：独立开发、内部使用的教学工具。文档目标是**让程序员（或 AI 编码助手）读完就能知道每一块要做什么**。  
> 配套文档：`docs/开发计划-组件复用与实施.md`（怎么干、按什么顺序干）、`docs/design/guitar-home/design-spec.md`（视觉与交互规范 v2.0）、`docs/design/guitar-home/*.html`（页面设计稿）。

## 0. 怎么读这份文档

1. **先读 §1–§4**：全局规则（角色、权限、账号、通用页面规范、内容模型），所有模块都受它约束。
2. **再读 §5 你要做的模块**：每个模块固定六段——目标 / 入口与权限 / 页面与交互 / 数据与接口 / 规则与边界 / 验收标准。
3. **动手前确认三件事**：模块状态（见下方图例）、参照哪份设计稿、复用哪些已有代码。
4. **视觉细节不在这里重复**：颜色、字号、间距、动效一律以 `design-spec.md` v2.0 和对应设计稿 HTML 为准，PRD 只写业务要求。

状态图例（贯穿全文）：

|  标记 | 含义                       |
| :-: | ------------------------ |
|  ✅  | 已上线可用，改动需谨慎              |
|  ⚠️ | 功能已在，但**视图层未按设计稿实现，需返工** |
|  ❌  | 未实现，待开发                  |
|  ➖  | 本轮暂缓，最后处理                |


### 0.1 模块 → 文档 → 代码 总索引

| 模块     |   本节  | 设计稿                                                              | 主要代码文件                                                                           |  开发计划 |
| ------ | :---: | ---------------------------------------------------------------- | -------------------------------------------------------------------------------- | :---: |
| 页头导航   |  §3.1 | 全部设计稿的 nav                                                       | `frontend/components/SiteHeader.js`                                              | M2/M6 |
| 曲目区    |  §5.1 | `tracks-home.html`、`track-point-detail.html`、`track-collection-detail.html` | `frontend/app/tracks/**`、`backend/src/tracks.routes.js`                          | M2/M3 |
| 知识点区   |  §5.2 | `knowledge-home.html` + 与曲目区同构稿                                  | `frontend/app/knowledge/**`、`backend/src/knowledge.routes.js`                    |   M4  |
| 图书馆    |  §5.3 | `library.html`、`library-book.html`、`admin-upload-pdf.html`       | 全缺（新建）                                                                           |   M7  |
| 内容创建   |  §5.4 | `admin-create-entry.html`、`admin-create-track-point.html`、`admin-create-track-collection.html` | `frontend/app/admin/upload/page.js` 等                                            |   M3  |
| 视频上传   |  §5.5 | 无设计稿（能力复用）                                                       | `frontend/components/uploadVideo.js`、`backend/src/videos.routes.js`、`storage.js` | 全局硬约束 |
| 学生管理   |  §5.6 | `student-manage.html`                                            | `frontend/app/admin/users/**`                                                    | M5/M6 |
| 班级管理   |  §5.7 | `class-manage.html`                                              | `frontend/app/admin/classes/page.js`                                             |   M6  |
| 作业推送   |  §5.8 | `student-manage.html` 作业区                                        | `backend/src/admin.routes.js`                                                    |   M5  || 学员首页   |  §5.9 | `student-home.html`                                              | `frontend/app/page.js`                                                           |   M8  |
| 我的历史作业 | §5.10 | `my-homework.html`                                               | 新建 `frontend/app/my-homework/page.js`                                            |   M8  |
| 登录与游客  | §5.11 | `login.html`                                                     | `frontend/app/login/**`、`frontend/lib/guest.js`                                  |   M6  |

> 设计稿命名已统一（2026-09-12）：全部对齐 §4.4 术语，旧名作废——`course-detail.html` → **`track-point-detail.html`**、`collection-detail.html` → **`track-collection-detail.html`**、`admin-create-lesson.html` → **`admin-create-track-point.html`**、`admin-create-collection.html` → **`admin-create-track-collection.html`**、`admin-create-content.html` → **`admin-create-entry.html`**。设计稿内部互跳链接已同步，文档引用一律用新名。

---

## 1. 产品定位与范围

面向吉他教室的**内部教学视频平台**。三类使用者：

- **教导主任**：上传与维护全部教学内容（曲目、知识点、资料），管理账号与班级
- **老师**：管理本班学生，查看学习记录，推送作业与留言
- **学员 / 游客**：看视频、下载资料，学员额外能看到老师布置的作业

范围边界：

- 账号**由教导主任统一发放**，无注册、无自助改密、无找回密码
- 不收费、不做评论弹幕、不做视频转码/多清晰度、不做数据看板
- 前端 Web（校园/教室内使用），设计方案为桌面优先；移动端仅要求不崩（见 §3.4 响应式）

---

## 2. 角色、权限与账号

### 2.1 角色定义

| 角色   | 说明       | 存储位置                              |
| ---- | -------- | --------------------------------- |
| 游客   | 未登录访问者   | 无账号（前端 `guest.js` 控制）             |
| 学员   | 付费/在册学生  | `users` 表                         |
| 老师   | 带班教师     | `admins` 表，`role = 'teacher'`     |
| 教导主任 | 内容与账号管理者 | `admins` 表，`role = 'super_admin'` |

> 实现提示：老师与教导主任**同表不同 role**，登录后由 `role` 字段决定前端口径；学员独立在 `users` 表，登录走 `/api/user/login`。

### 2.2 权限矩阵

| 能力 / 板块                        |  游客 |  学员 |     老师    | 教导主任 |
| ------------------------------ | :-: | :-: | :-------: | :--: |
| 播放视频、下载资料                      |  ✅  |  ✅  |     ✅     |   ✅  |
| 曲目区 / 知识点区 / 图书馆 浏览            |  ✅  |  ✅  |     ✅     |   ✅  |
| 内容上传 / 编辑 / 删除（曲目、知识点、合集、P、资料） |  ❌  |  ❌  |     ❌     |   ✅  |
| 首页（作业 + 留言）                    |  ❌  |  ✅  |     ❌     |   ❌  |
| 我的历史作业                         |  ❌  |  ✅  |     ❌     |   ❌  |
| 学生管理                           |  ❌  |  ❌  |  ✅ 限本人班级  | ✅ 全部 |
| 布置作业 / 留言                      |  ❌  |  ❌  | ✅ 限本人班级学生 | ✅ 全部 |
| 停用 / 恢复本班学生                    |  ❌  |  ❌  |     ✅     |   ✅  |
| 班级管理（建账号、分班、停用/删除账号）           |  ❌  |  ❌  |     ❌     |   ✅  |

前端**不渲染越权入口**，后端**每个接口独立鉴权**（前端隐藏不算安全措施）。

### 2.3 核心规则

1. **内容权与管理权分离**：老师**零内容权限**，一切内容增删改仅教导主任。
2. **老师与教导主任没有首页**：老师登录后默认进入「学生管理」；教导主任登录后默认进入「曲目区」。
3. **游客没有首页**：游客入口进入后默认落在「曲目区」，只能播放与下载。
4. **学生管理是同一个页面**：顶部两个筛选条件——**班级 + 学生**（学生可搜索）。老师进入时班级筛选**锁死为本人所带班级**（一师多班时可多选）；教导主任两个筛选均自由。选定学生后的学习记录、布置作业为**同一套组件**。
5. **分班结构**：一个老师可对应多个班级（`teacher_classes` 多对多）；一个学生只属于一个班级（`users.class_id`）。
6. **内容全局共享**：教导主任上传的曲目、知识点、资料对所有角色可见（游客可播放/下载）。
7. **停用 ≠ 删除**：被停用账号仍可正常登录，只是进入受限状态（见 §2.6）。
8. **详情页不做学习进度**：无「已看 ✓」、无进度条/百分比；前置知识点也不做「未学习」标记（**此项与 design-spec v2.0 §2.4 的绿色未学习圆点冲突，以用户要求为准：本轮不实现该状态标记**）。

### 2.4 账号模型

四要素，`users` 与 `admins` 两张表字段口径一致：

| 字段              | 类型            | 说明                             |
| --------------- | ------------- | ------------------------------ |
| `role`          | varchar       | 学员 / 老师 / 教导主任（`users` 表隐含为学员） |
| `account`       | varchar, 全局唯一 | **登录账号**（如手机号），登录凭据之一          |
| `nickname`      | varchar       | 昵称，**仅界面展示**（学生列表、留言署名）        |
| `password_hash` | text          | 密码（bcrypt）                     |
| `status`        | varchar       | `active` / `disabled`          |

规则：

- 登录方式 = **账号 + 密码**，昵称不参与登录（此前把 `username` 当登录名是错的）
- 现有 `username`（如 `student_a1`）**本质是昵称**，保留展示用途
- 本轮**不做旧数据迁移**：开发前清空测试数据，按新模型重新初始化（见开发计划 M0）

### 2.5 登录与会话

- 三类身份**共用登录入口**语义：学员走 `/api/user/login`，老师/教导主任走 `/api/admin/login`；登录页按同一界面呈现，提交后由后端返回的角色决定跳转
- 会话凭据沿用现有 cookie 机制（`auth.js`），不做 token 改造
- 登录成功后跳转规则：学员 → 首页 `/`；老师 → `/admin/users`；教导主任 → `/tracks`
- 游客入口：登录页提供「游客浏览」入口，进入后落 `/tracks`

### 2.6 停用机制

`status = 'disabled'` 的账号**仍可登录**，行为如下：

| 角色   | 表现                                                                 |
| ---- | ------------------------------------------------------------------ |
| 学员   | 仍可浏览视频、下载资料；**首页**显示提示条；**我的历史作业**页显示提示条（文案见 §8）                   |
| 老师   | 仍可登录，`/admin/users` 内容区替换为停用提示（标题 + 正文见 §8），**不可执行任何管理操作**，接口层同样拒绝 |
| 教导主任 | 可正常登录；默认收起内容管理入口（保留只读浏览），是否保留管理权由站长决定                              |

操作权限与入口：

- 停用 / 恢复**学员**：本班老师 或 教导主任
- 停用 / 删除**老师、其他教导主任**：仅教导主任
- 恢复与停用**同入口，一键切换**（`PATCH /api/admin/users/:id/status`）
- 停用老师后，其带班关系保留（`teacher_classes` 不清空），恢复后原样生效

---

## 3. 全局页面规范

### 3.1 页头导航（AppNav）

**唯一导航组件**，全站页面共用。数据来源 = 当前角色。

| 角色   | 页头项                    |
| ---- | ---------------------- |
| 游客   | 曲目区、知识点区、图书馆（+ 右侧「登录」） |
| 学员   | 首页、曲目区、知识点区、图书馆、我的历史作业 |
| 老师   | 曲目区、知识点区、图书馆、学生管理      |
| 教导主任 | 曲目区、知识点区、图书馆、学生管理、班级管理 |

要求：

- 激活态 = 当前频道高亮（墨黑 + 底部橘色下划线），**跨频道链接必须真实跳转到对应首页**
- 页头**不放搜索入口**（搜索只在页面内，见 §3.2）
- 右上角头像 34px + 在线绿点；点击展开菜单（当前账号昵称、退出登录）
- ⚠️ 现状 `SiteHeader.js` 未按此矩阵实现，需返工（含角色判断与游客态）

### 3.2 列表页通用规范（曲目区首页 / 知识点区首页 / 图书馆共用）

以下能力三页**共用同一套组件**，只换数据源与文案：

| 组件    | 要求                                                                                             |
| ----- | ---------------------------------------------------------------------------------------------- |
| 搜索条   | 页级唯一搜索入口，位于导航下方工具行左侧；白底 + coffee 描边 + 放大镜 icon + 输入时的「清除」按钮；**一条搜索框同时过滤本页两个区**；搜索变化时分页重置回第 1 页 |
| 排序下拉  | 选项：按更新时间倒序（默认）/ 更新时间正序 / 按首字母（zh-CN 拼音序）；**每个区独立排序**，切换后该区回到第 1 页                              |
| 卡片网格  | 每行 8 张，卡片等高；卡片内容 = 封面（1:1）+ 名称 + 更新时间；**不带任何状态角标**                                             |
| 悬停操作层 | **仅教导主任**：卡片 hover 居中显示 编辑 / 删除 / 下载 / 查看详情；删除**必须二次确认**                                       |
| 翻页器   | 「上一页 / 第 X 页 · 共 Y 页 / 下一页 / 跳转到 X 页」；**单页也常驻显示**（共 1 页时上下页禁用）                                 |
| 空态    | 两级：分区空态（「XX 中未找到与「关键词」相关的内容」）+ 两区全空的全局空态卡片（含「清除搜索」按钮）                                          |

### 3.3 详情页通用模板（曲目 / 知识点共用）

一屏完成，**页面级不滚动**，滚动只发生在盒内（`.scroll`）。

```
导航 56px
└─ .detail（居中，padding 16px 36px 20px）
   ├─ 左列 .main      ← 合并为一个大盒，内部虚线分隔：
   │                    面包屑 → 标题 → 前置知识点 → 播放器(16:9) → 简介
   └─ 右列 .side 300px
      ├─ 播放列表（P 列表，flex 更高）
      ├─ 附件资料（可下载）
      └─ 关联图书馆（紧凑，展示关联状态）
```

要求：

- 播放器：HTML5 video，纯白实底 + coffee 描边 + 无阴影；倍速、上/下段为**可选项，不作为验收项**；**清晰度切换为必做**（见 §4.7：档位菜单只列已就绪档位，**默认播放 1080p**，切换保留播放进度）
- 播放列表：显示序号、P 名、时长；**正在播放的 P 高亮**即可；无进度条、无已看标记
- 面包屑示例：「渐入佳境 · 第 3 段」/「指弹入门 · 第 2 段」
- 同一套模板服务四个页面：`/tracks/[id]`、`/knowledge/[id]`、`/tracks/collections/[id]`、`/knowledge/collections/[id]`

### 3.4 视觉规范引用（摘要，细节以 design-spec.md v2.0 为准）

- **页面底色纯白** + 焦糖咖啡格纹底饰（下浓上淡、页头处消失）；**旧版米黄纸底已废除**
- 层级：L1 容器（1.5px coffee 描边 + 玻璃底 + 轻投影）/ L2 卡片（1px hair-2 细边 + 纯白 + 轻投影）
- 主色 coffee `#5d4326`，强调亮橙 `#ff7f00`，**绿色只做点睛**（当前播放行、eq 条、头像在线点）
- 圆角：盒子 6px、小控件 4px；**禁止大圆角/胶囊**；**禁止硬偏移投影**
- 缓动统一 `cubic-bezier(.16,1,.3,1)`；删除操作一律二次确认
- 响应式：≤1024px 网格降列、详情页恢复页面滚动；≤640px 页边距收窄、隐藏导航链接
- 登录页为独立水彩明信片风格，不适用上述背景与导航规范

> ⚠️ 注意：开发计划 §0 中「纸底 + 咖啡描边」的表述是 v1 旧规范，**以本节的 v2.0 纯白底为准**，返工时不要照抄旧描述。

---

## 4. 内容模型

### 4.1 三段式对象层级

```
单课（track_point / knowledge_point）   例：卡农 / 1645 和弦进行
 ├─ 封面（1:1，必填）
 ├─ 简介
 ├─ P1 ── video（复用 videos 表）
 ├─ P2 ── video
 └─ P3 ── video
合集（track_collection / knowledge_collection）  例：周杰伦专辑 / 指弹入门
 ├─ 封面（1:1，必填）
 ├─ 简介
 └─ 收录多个单课（多对多：track_collection_items / knowledge_collection_items）
```

要点：

- **P 不是独立内容**：P 依附单课存在（`track_parts` / `knowledge_parts`），本身**没有封面、没有简介页**
- **单课可同时属于多个合集**，互不影响；删除合集只删关联，不删单课
- **删除单课时**：同步删除其 P 关联的 `video` 与 COS 对象（沿用现有删除一致性链路）

### 4.2 封面规则

| 对象             |          封面          |
| -------------- | :------------------: |
| 单课（单曲目 / 单知识点） |        需要，1:1        |
| 合集（曲谱集 / 知识点集） |        需要，1:1        |
| P 分段           | **不需要**（展示时用所属单课的封面） |

- 上传单课/合集时封面**必填**（沿用现有封面必填校验）
- 推送单个 P 时，卡片展示**所属单课的封面**
- 因此 `track_parts` / `knowledge_parts` **不设 cover 字段**

### 4.3 前置关系

- 详情页「前置知识点」可选**四种对象**：单曲目、曲谱集、单知识点、知识点集
- 点击前置项跳转到对应详情页（合集类前置跳到合集详情页）
- **不做环判定**：老师在后端手动维护，出现环时按实际跳转链浏览，不做检测与提示
- 存储统一走 `prerequisites` 通用表（`object_type + object_id → prerequisite_type + prerequisite_id`），两种内容区共用
- 唯一的自校验：不允许自己指向自己（表级 CHECK 已存在）

### 4.4 object_type 枚举（**全站统一，以此表为准**）

| 值                      | 含义              | 对应表                 |
| ---------------------- | --------------- | ------------------- |
| `video`                | 旧单视频（兼容历史，不再新建） | `videos`            |
| `track_point`          | 单曲目             | `track_points`      |
| `track_collection`     | 曲谱集             | `track_collections` |
| `track_part`           | 曲目分 P           | `track_parts`       |
| `knowledge_point`      | 单知识点            | `knowledge_points`  |
| `knowledge_collection` | 知识点集            | `knowledge_collections`    |
| `knowledge_part`       | 知识点分 P          | `knowledge_parts`          |
| `library_resource`     | 图书馆资料           | `library_resources` |

> ⚠️ 命名定稿（2026-09-11 晚）：**两区命名完全对称**——曲目区 `track_point` / `track_collection`，知识点区 `knowledge_point` / `knowledge_collection`；**数据库表名同步重命名，禁止一切简写**（`kp_*` 前缀已废除，表已改名为 `knowledge_*` / `track_*` 全称）。早期用过的 `track` / `collection` / `kp_collection` / `kp_part` 全部作废。前后端代码已按本表完成重命名，旧库直接清空重建（M0 约定）。新增接口前先核对此表。

### 4.5 附件与资料

两类不同的东西，不要混：

| 类型    | 归属              | 表                                                                         | 说明                            |
| ----- | --------------- | ------------------------------------------------------------------------- | ----------------------------- |
| 内容附件  | 挂在单课/合集下，随详情页展示 | `content_attachments`（object_type + object_id）；旧 `video_attachments` 保留兼容 | PDF / GP / MP3 / PNG 等，逐个下载   |
| 图书馆资料 | 独立资料库，可被内容关联    | `library_resources` + `library_resource_links`                            | 有独立浏览页与上传页；详情页右下「关联图书馆」展示关联状态 |

### 4.6 P 命名与展示规范（布置作业搜索依赖）

**背景**：布置作业要能搜到并精准选中某个 P（如卡农里的「基本功练习」）。P 名单独看（「基本功练习」）无法知道属于哪首曲子，因此**所有跨上下文的 P 展示与搜索场景，一律拼接父级名**：

```
显示名 = 单课名-P名        例：卡农-基本功练习
                            1645 和弦进行-击弦练习
```

- **存储**：`track_parts` / `knowledge_parts` 的 `name` 只存 P 名本身（如「基本功练习」），**不存拼接结果**；拼接在展示层做
- **必须拼接的场景**（否则 P 名有歧义）：
  1. 布置作业的推送对象搜索结果（§5.8）
  2. 学员首页 / 历史作业的**单 P 作业卡片标题**
  3. 老师删除/取消作业时的确认提示
- **不拼接的场景**（上下文已有父级）：
  1. 单课详情页右侧 P 播放列表（已在卡农页面内，只写「基本功练习」）
  2. 新建/编辑单课时的 P 管理行
- 分隔符固定半角 `-`；P 名与单课名之间不再加空格
- 搜索匹配规则：搜「卡农」能命中其所有 P；搜「基本功练习」也能命中，结果里显示「卡农-基本功练习」

### 4.7 视频清晰度（自动转码 + 多档播放）

**方案定稿：教导主任只上传原画文件，服务端自动转码出 1080p / 720p，播放器手动切换档位。**

- **档位枚举（固定三种，全站统一）**：

| quality 值 | 含义       | 来源                                   |
| ---------- | ---------- | ------------------------------------ |
| `source`   | 原画         | 上传时的原始文件，**必传，上传完成即可播**    |
| `1080p`    | 高清 1080p | **服务端自动转码生成**，默认播放档        |
| `720p`     | 流畅 720p  | **服务端自动转码生成**                  |

- **数据模型**：新增表 `video_renditions`（`video_id → videos.id` ON DELETE CASCADE、`quality`、`video_url`、`file_size`、`status`，`UNIQUE(video_id, quality)`）；`status ∈ ready / processing / failed`；`videos.video_url` 保留为原画档，**旧路径不受影响**
- **转码实现（定稿：用腾讯云数据万象 CI，不自建转码）**：
  - COS 本身不带转码；**开通数据万象（Cloud Infinite）并绑定存储桶**后即可用其媒体处理能力（付费服务，有少量免费额度，超出按转码时长计费）
  - 在 CI 控制台建 **1080p / 720p 两个自定义转码模板** + 一条**数据工作流**（绑定 `videos/` 输入路径）——文件上传到该路径即**自动触发转码**，产物自动保存到约定输出路径（`videos/{videoId}/{quality}/`），**无需自写 ffmpeg**
  - 后端只做**胶水**：接收 CI 工作流的**业务回调**（或轮询任务状态），把对应 rendition 置 `ready` / `failed`；CI 回调接口用现有鉴权（签名校验）
  - **服务状态与转码解耦**：CI 不可用 / 未开通时，系统照常工作，只是只有原画档（原画永远可播）
- **播放器交互**（详情页通用模板 §3.3）：
  - 档位菜单**只列 `ready` 的档位**，当前档高亮；**默认播放 1080p**
  - 1080p 尚未转出（processing）时**回退原画播放**，转出后刷新页面可切
  - 切换档位**保留 currentTime 与播放状态**，不重新加载页面
- **规则与边界**：
  - 转码产物为渐进式 MP4，**不做 HLS/DASH 码率自适应**，切换即换文件地址
  - 删除 video 时 renditions 级联删除，COS 对象（含各档产物）沿用现有删除一致性链路逐一清理
  - 转码是尽力而为的增强功能：**转码失败或 CI 未开通不阻塞上传与播放**（原画永远可播）
  - 附件、封面与清晰度无关
  - **运维前置（控制台操作，非代码）**：① 开通数据万象并绑定存储桶；② 建 1080p/720p 转码模板；③ 建工作流绑定 `videos/` 路径并配置业务回调 URL

### 4.8 内容删除与作业快照策略（2026-09-12 定稿）

- **物理删除**：内容（视频/单课/合集/知识点/曲谱/电子书资料）删除即真删（库记录 + COS 对象），无回收站、无「已失效」占位态；删除入口一律**二次确认**。
- **引用直接消失**：删除时级联清理 `track_collection_items` / `knowledge_collection_items`、`prerequisites`、`content_attachments`、`library_resource_links` 中的关联记录，合集收录数等计数随之重算，不留任何残留标记。
- **作业零引用快照**：`user_assignments` 推送时即固化**标题快照**（含类型标识），此后与源内容无任何关联；源内容删除不影响作业记录本身：
  - 学员端「今日作业」：按内容是否存活实时过滤，被删项直接消失；全部被删即显示空态；
  - 老师端「学习记录」：纯文本浏览（标题快照 + 留言 + 时间），**无点击行为、无「已删除」特殊状态**。
- **删除操作日志**：物理删除无回收站、不可撤销，所有删除操作需写日志（时间/操作人/对象），作为唯一追溯手段。
- **无学习行为统计**：全站不统计学习进度、观看时长等任何收看数据（见 §9）。

---

## 5. 功能模块

### 5.1 曲目区

**状态**：后端 ⚠️ 接口齐备；前端 ⚠️ 页面在、未按设计稿实现。**本轮以 UI 返工为主**。

#### 5.1.1 目标

教导主任把教学视频组织成「单曲目 + 曲谱集」；所有角色可浏览、播放、下载。

#### 5.1.2 入口与权限

| 页面    | 路由                         |   游客/学员/老师   |     教导主任    |
| ----- | -------------------------- | :----------: | :---------: |
| 曲目区首页 | `/tracks`                  | 浏览 + 搜索 + 排序 |  + 新建/编辑/删除 |
| 单曲目详情 | `/tracks/[id]`             |   播放 + 下载附件  | + 管理 P、编辑信息 |
| 曲谱集详情 | `/tracks/collections/[id]` |    浏览收录曲目    |    + 编辑合集   |

#### 5.1.3 页面与交互

**首页**（参照 `tracks-home.html`）：

- 上下两区：上半「曲目」、下半「曲谱集」，**各自独立排序与独立分页**（设计稿两个 pager）
- 一条搜索框同时过滤两个区；空态两级（见 §3.2）
- 教导主任可见右上「新建单曲目 / 新建曲谱集」按钮

**点击链路**（必须严格按此实现，不允许合集卡片直进单课）：

- 点**曲目卡片** → `/tracks/[id]` 单曲目详情页
- 点**曲谱集卡片** → `/tracks/collections/[id]` 曲谱集详情页 → 点集内曲目卡片 → 单曲目详情页

**单曲目详情**（参照 `track-point-detail.html`）：按 §3.3 模板，右列三盒 = 播放列表 + 附件资料 + 关联图书馆。

**曲谱集详情**（参照 `track-collection-detail.html`）：

- 面包屑（曲目库 / 合集名）+ 合集头部：封面（1:1）+ 名称 + 简介 + meta（收录数量等）
- 「收录单曲」网格：卡片字段 = 序号、曲名、时长、`N 个 P`；点击进单曲目详情
- 合集网格同样分页（复用 §3.2 pager）

#### 5.1.4 数据与接口

已有表：`track_points` / `track_parts` / `track_collections` / `track_collection_items`（见 §6）。  
已有接口（`/api/tracks`，全部 `requireSuperAdmin` 保护写操作）：

| 方法     | 路径                                                              | 说明                           |
| ------ | --------------------------------------------------------------- | ---------------------------- |
| GET    | `/api/tracks`                                                   | 曲目 + 曲谱集列表（搜索、排序、分页参数）       |
| GET    | `/api/tracks/:id`                                               | 单曲目详情（含 P 列表、附件、前置、关联图书馆）    |
| GET    | `/api/tracks/collections/:id`                                   | 曲谱集详情                        |
| POST   | `/api/tracks`                                                   | 新建单曲目                        |
| PATCH  | `/api/tracks/:id`                                               | 编辑单曲目                        |
| DELETE | `/api/tracks/:id`                                               | 删除单曲目（连带 P 的 video 与 COS 对象） |
| POST   | `/api/tracks/:id/parts`                                         | 新增 P                         |
| PATCH  | `/api/tracks/:id/parts/:partId`                                 | 编辑 P（含标题、排序）                 |
| DELETE | `/api/tracks/:id/parts/:partId`                                 | 删除 P                         |
| POST   | `/api/tracks/:id/parts/upload/{create,part-url,complete,abort}` | **P 视频分片直传**（复用现有协议）         |
| POST   | `/api/tracks/collections`                                       | 新建曲谱集                        |
| PATCH  | `/api/tracks/collections/:id`                                   | 编辑曲谱集                        |
| DELETE | `/api/tracks/collections/:id`                                   | 删除曲谱集（只解除关联，不删单课）            |
| PATCH  | `/api/tracks/collections/:id/tracks`                            | 设置合集内的曲目（增删/排序）              |

#### 5.1.5 规则与边界

- 一个曲目可进入多个曲谱集；删除曲谱集**不得**删除曲目本身
- 删除曲目必须走现有删除一致性链路（**先删 COS 对象再删库记录**），禁止只删库
- P 的序号 `part_no` 在同一曲目内唯一（表级 UNIQUE），调整顺序时需避免中间态冲突
- 详情页不记录任何播放状态，**不新增进度表**

#### 5.1.6 验收标准

1. 首页上下两区正确显示；一条搜索框同时命中曲目名与曲谱集名；两区可独立排序，默认更新时间倒序
2. 点曲目进详情页，P 列表可切换播放；从曲谱集内点曲目同样进入
3. 同一曲目出现在多个曲谱集中互不影响；删除曲谱集后曲目仍在
4. 详情页右列三盒齐备；P 列表无进度条、无已看标记
5. 封面 1:1；P 无封面上传入口
6. 老师看不到任何新建/编辑/删除按钮；直接调写接口返回无权限
7. 视觉与 `tracks-home.html` / `track-point-detail.html` 逐块一致
8. **点曲谱集卡片进的是曲谱集详情页**（不是直接进某首曲目）；集内卡片点击才进单曲目详情

---

### 5.2 知识点区

**状态**：后端 ⚠️ 接口齐备；前端 ⚠️ 页面在、未按设计稿实现。

- **目标**：与曲目区**完全同构**，只是对象换成「单知识点 + 知识点集」
- **入口与权限**：同 §5.1.2，路由为 `/knowledge`、`/knowledge/[id]`、`/knowledge/collections/[id]`
- **页面与交互**：首页参照 `knowledge-home.html`；详情页与合集页复用曲目区模板（§3.3）；知识点卡片加轻量「知识点」小标签以便与曲目卡区分
- **点击链路**：与 §5.1.3 相同——点**知识点卡片**进单知识点详情；点**知识点集卡片**进 `/knowledge/collections/[id]` 知识点集详情页（参照 `knowledge-collection-detail.html`，「收录单课」网格 = 序号/名称/时长/N 个 P），点集内卡片再进单课详情；两个合集详情页结构同构、复用同一模板
- **数据与接口**：表 `knowledge_points` / `knowledge_parts` / `knowledge_collections` / `knowledge_collection_items`；接口 `/api/knowledge/*`，与曲目区一一对应（列表/详情/增删改/P 直传/合集）
- **规则与边界**：与 §5.1.5 完全一致；前置关系走同一张 `prerequisites`
- **验收标准**：
  1. 与曲目区视觉同构且两区可明显区分
  2. 详情页、合集页、P 播放全部跑通
  3. 前置关系读写走 `prerequisites`，不再使用 `video_prerequisites`
  4. 同样的越权拦截（写操作仅教导主任）

> 实现建议：目标是**曲目区与知识点区共用同一套页面模板与组件**，仅数据源、路由前缀、卡片标签不同。不允许为知识点区另写一套样式。

---


### 5.3 图书馆

**状态**：❌ 未实现（**数据库表已建**：`library_resources`、`library_resource_links`；**后端无路由文件，前端无页面**）。

- **目标**：独立的资料库，集中浏览与下载所有教学资料（电子书、曲谱 PDF、伴奏等）；同时作为内容详情页「关联图书馆」的数据源
- **入口与权限**：
  - 浏览页 `/library`：游客/学员/老师/教导主任均可浏览下载
  - 上传页：**仅教导主任**（参照 `admin-upload-pdf.html`）
- **页面与交互**：
  - 浏览页（参照 `library.html`）：卡片网格（A4 卡形态，见 design-spec §4 L2），带搜索、排序、翻页（复用 §3.2 组件）；每页行数按框高自适应但**封顶 2 排**，`align-content:center`；搜索框 placeholder「搜索资料名称…」
  - **图书详情页**（参照 `library-book.html`，`/library/[id]`，从浏览页点卡片进入）：封面（chip 标注「图书馆 · PDF 资料」）+ 标题 + 元信息行（`N 页 · X MB · 上传时间`，设计稿格式如「10 页 · 2.9 MB · 3 周前」）+ 上传人 + 简介 + 下载按钮
  - 上传页：标题、简介、封面、选择文件（**小文件走 `uploadBlobWithProgress`，不走分片**）、可选关联内容对象
  - 卡片悬停（教导主任）：编辑/删除/下载/查看详情 + 二次确认
- **数据与接口**：
  - 需**新建** `backend/src/library.routes.js`，挂载 `/api/library`
  - 接口清单（建议）：`GET /api/library`（列表）、`GET /api/library/:id`、`POST /api/library`、`PATCH /api/library/:id`、`DELETE /api/library/:id`、`POST /api/library/upload-url`（预签名小文件直传）、`PATCH /api/library/:id/links`（设置关联对象）
  - 删除资料时同步删除 COS 对象与 `library_resource_links` 关联
- **规则与边界**：
  - 资料是**独立对象**，可被多个单课/合集关联（`library_resource_links` 多对多）
  - 删除资料 = 删除关联 + 对象存储文件；**不得**影响关联的内容对象
  - 详情页「关联图书馆」只展示关联状态并可跳转，不复制文件
- **验收标准**：
  1. 游客可浏览并下载资料
  2. 教导主任可上传、编辑、删除资料；删除后 COS 对象与关联一并清理
  3. 曲目区/知识点区详情页「关联图书馆」盒接入真实数据并可跳转
  4. 搜索、排序、翻页与两区首页行为一致

---

### 5.4 内容创建（教导主任）

**状态**：⚠️ 页面在，流程与设计稿不符，需返工为三步流。

- **目标**：教导主任创建内容的统一入口，覆盖「选区 → 选类型 → 填表单」完整流程
- **入口与权限**：仅教导主任。**取消独立的「新建课程」页头入口**，改为在曲目区/知识点区内按角色显示「新建」按钮，点击进入统一创建页
- **页面与交互**（参照 `admin-create-entry.html` + `admin-create-track-point.html` + `admin-create-track-collection.html`）：
  1. **第一步 选区**：知识点区 / 曲目区（`admin-create-entry.html` 是路由页：读 `?zone=` 参数后跳转对应新建页，前端实现为 `/admin/create?zone=track|knowledge` 的中转）
  2. **第二步 选类型**：新建单课 / 新建合集
  3. **第三步 填表单**：
     - 单课表单（`admin-create-track-point.html`）：名称（placeholder「如：卡农（Johann Pachelbel · 指弹改编）」）、简介（「本课的教学目标、适合阶段、练习建议…」）、封面（1:1 必填）+ **P 分段管理区**（每行 = P 名（placeholder「P 名，如：完整示范 · 原速」）+ **视频上传（只传一个原画文件，1080p/720p 由服务端自动转码，见 §4.7）** + 上移/下移/删除；底部「添加 P」；P 无封面）+ 前置知识点选择（四种对象多选，搜索 placeholder「搜索单知识点 / 知识点集 / 单曲目 / 曲谱集…」）
     - 合集表单（`admin-create-track-collection.html`）：名称、简介、封面（1:1 必填）+ 从已有单课中多选加入
- **数据与接口**：复用 §5.1.4 / §5.2 的创建接口；P 视频上传调用 `POST /api/{tracks|knowledge}/:id/parts/upload/*`
- **规则与边界**：
  - 单课必须先建主体、拿到 id 后再传 P（或先本地暂存，提交时批量创建）
  - 封面必填；P 不需要封面
  - 前置知识点可选四种对象，允许成环，不做校验提示
- **验收标准**：
  1. 三步流程可完整走通，任意一步可回退
  2. 单课 + 多 P 一次创建成功；P 可增删与调序
  3. 合集可从已有单课多选；同一单课可加入多个合集
  4. 表单校验：名称为空、封面未传、P 视频未传均有明确提示
  5. 老师/学员/游客访问该路由被拦截

---

### 5.5 视频上传与 COS 分片直传（通用能力）

**状态**：✅ 已完整实现，**只许调用，不许重写**。这是本项目复用优先级最高的模块。

| 层     | 文件                                                                   | 能力                                                                              | 允许的改动                                                                                    |
| ----- | -------------------------------------------------------------------- | ------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------- |
| 前端上传  | `frontend/components/uploadVideo.js`                                 | 校验常量、`validateUploadForm`、`uploadVideoByMultipart`（8MB 切片 / 并发 / 进度 / 失败 abort） | **无**。分 P = 对每个 P 循环调用一次                                                                 |
| 前端小文件 | `frontend/lib/api.js` → `uploadBlobWithProgress`                     | 封面、附件、PDF 预签名直传                                                                 | **无**                                                                                    |
| 后端    | `backend/src/videos.routes.js`（create / part-url / complete / abort） | 分片直传四步                                                                          | 仅按业务加**归属参数与 key 前缀**（`tracks/...`、`knowledge/...`、`covers/...`、`library/...`）+ **quality 参数**（同一 P 多档 = 多次直传，见 §4.7）；**不改变分片协议与直传流程** |
| 后端    | `backend/src/storage.js`                                             | 预签名、HeadObject 校验、删除、列对象                                                        | **无**（清空存储桶属初始化步骤）                                                                       |

❌ 明确禁止：重写分片直传、另写第二套上传逻辑、把文件改为走后端中转、改动 `storage.js` 的校验与删除顺序。

**验收**：分 P 上传与封面/附件上传均走既有链路；上传失败可 abort 且不产生孤儿文件；key 前缀按业务正确归类。

---


### 5.6 学生管理

**状态**：⚠️ 页面在，需按 `student-manage.html` 返工（含双筛选与停用态）；后端接口 ✅ 基本齐备。

- **目标**：老师/教导主任查看学生学习记录并布置作业（推送视频 + 留言）
- **入口与权限**：老师、教导主任（页头「学生管理」）。**游客/学员不可见**
- **页面与交互**（参照 `student-manage.html`）：
  - 顶部两个筛选：**班级 + 学生**（学生可搜索）；**老师进入时班级筛选锁死本人班级**（一师多班可多选）；教导主任两个筛选自由
  - 选中学生后内容区三块：**学习记录**（该生的作业/操作历史，按 operation 聚合；**纯标题快照、纯浏览不可点击**，见 §4.8）、**布置作业**（推送对象选择 + 留言）、**账号操作**（停用/恢复，老师限本班、教导主任不限）
  - **老师被停用态**：内容区替换为停用提示（标题「此账号已停用」+ 正文见 §8），不显示任何学生数据与操作
  - **老师暂无带班态**：提示「请联系教导主任获取工作内容」（见 §8）
- **数据与接口**（现有 `/api/admin/*`）：

| 方法    | 路径                                     | 权限            | 说明              |
| ----- | -------------------------------------- | ------------- | --------------- |
| GET   | `/api/admin/users`                     | 老师（限本班）/ 教导主任 | 学生列表（按班级筛选、搜索）  |
| GET   | `/api/admin/users/:userId/assignments` | 同上            | 学生的学习记录         |
| POST  | `/api/admin/users/:userId/assignments` | 同上            | 布置作业（推送，见 §5.8） |
| POST  | `/api/admin/users/:userId/message`     | 同上            | 老师留言            |
| PATCH | `/api/admin/users/:id/status`          | 同上            | 停用 / 恢复         |
| PATCH | `/api/admin/users/:id/class`           | 教导主任          | 调整学生班级          |

- **规则与边界**：
  - **老师只能访问本人班级学生**：前端锁筛选 + **后端按 `teacher_classes` 校验**（关键，不能只靠前端）
  - 停用学生不影响其登录与浏览（见 §2.6）
  - 一个页面服务两种角色，**不拆页面、不拆步骤**
- **验收标准**：
  1. 老师进入页面时班级筛选锁死本人班级，无法通过改参访问他班学生（后端拒绝）
  2. 教导主任可自由筛选班级与学生
  3. 学习记录与布置作业在同一页完成
  4. 老师停用态 / 暂无带班态文案逐字正确（§8）

---


### 5.7 班级管理

**状态**：⚠️ 页面在需返工；`admin/admins` 需并入本页后退役。

- **目标**：教导主任管理账号与班级
- **入口与权限**：**仅教导主任**（参照 `class-manage.html`）
- **页面与交互**：
  - 账号列表：学员 / 老师 / 教导主任分类查看，支持搜索与状态筛选
  - 新建账号表单：**角色、账号（手机号）、昵称、初始密码**（四要素，见 §2.4）
  - 分班：老师可对应**多个班级**（多对多）；学生归属一个班级
  - 账号操作：停用 / 恢复 / 删除 / 重置密码；删除需二次确认
- **数据与接口**（现有 `/api/admin/*`）：

| 方法     | 路径                                     | 说明                                          |
| ------ | -------------------------------------- | ------------------------------------------- |
| GET    | `/api/admin/users`                     | 学员列表                                        |
| POST   | `/api/admin/users`                     | 新建学员（account / nickname / password / class） |
| POST   | `/api/admin/users/:id/reset-password`  | 重置学员密码                                      |
| PATCH  | `/api/admin/users/:id/status`          | 停用 / 恢复                                     |
| PATCH  | `/api/admin/users/:id/class`           | 分配班级                                        |
| DELETE | `/api/admin/users/:id`                 | 删除学员                                        |
| GET    | `/api/admin/admins`                    | 老师/教导主任列表                                   |
| POST   | `/api/admin/admins`                    | 新建老师/教导主任                                   |
| POST   | `/api/admin/admins/:id/reset-password` | 重置密码                                        |
| DELETE | `/api/admin/admins/:id`                | 删除                                          |
| GET    | `/api/admin/classes`                   | 班级列表                                        |
| POST   | `/api/admin/classes`                   | 新建班级                                        |
| PATCH  | `/api/admin/classes/:id`               | 编辑班级（含带班老师）                                 |
| DELETE | `/api/admin/classes/:id`               | 删除班级                                        |

> 缺项：现有接口无「停用/恢复老师与教导主任」的入口（`admins` 表已有 `status` 字段）。需**新增** `PATCH /api/admin/admins/:id/status`。

- **规则与边界**：
  - 班级删除前需处理学生归属（置空或拒绝，二选一，建议**拒绝并提示先转移学生**）
  - 删除老师账号时其带班关系级联清理（`teacher_classes` 已配 ON DELETE CASCADE）
  - 至少保留一名教导主任（若删除最后一名，需拒绝）
- **验收标准**：
  1. 可新建学员/老师/教导主任，账号全局唯一校验生效
  2. 一师多班可配置；学生分班可改
  3. 停用/恢复/删除/重置密码均可用；删除有二次确认
  4. 非教导主任访问被拦截

---


### 5.8 作业推送

**状态**：后端 ✅ 已实现（支持多类型 + 单 P）；前端 ⚠️ 需按 `student-manage.html` 作业区返工。

- **目标**：老师/教导主任给学生推送学习内容并附留言
- **入口与权限**：学生管理页（§5.6）内；老师限本人班级学生
- **页面与交互**（参照 `student-manage.html` 作业区）：
  - 推送对象选择器：**复用前置知识点选择器组件**（同为四类对象：单曲目/曲谱集/单知识点/知识点集），并支持选到**单个 P**
  - 面板顶部**类型筛选**：全部 / 合集 / 单课 / 单 P（设计稿 `value=all|collection|lesson|part`）
  - 搜索框 placeholder「搜索合集 / 单课 / 单 P 名称…」；**单 P 在结果中显示为「单课名-P名」**（如「卡农-基本功练习」，规则见 §4.6），否则同名 P 无法区分
  - 可填留言（placeholder「作业备注 / 给学生的留言（选填）…」）；一次推送数量上限 **≤5**（沿用现有约束）
  - 取消/删除作业需**填写删除原因**（placeholder「请填写删除原因（如：手滑布置错了）…」），原因随软删除记录留存
- **推送粒度与跳转规则**：

| 推送对象       | object_type                           | 学员端点击后                |
| ---------- | ------------------------------------- | --------------------- |
| 曲谱集 / 知识点集 | `track_collection` / `knowledge_collection` | 进入合集详情页               |
| 单曲目 / 单知识点 | `track_point` / `knowledge_point`     | 进入详情页                 |
| 单个 P       | `track_part` / `knowledge_part`       | 进入所属单课详情页并**自动播放该 P** |
| 旧单视频       | `video`                               | 兼容保留（旧播放页退役后应逐步淘汰）    |

- **数据与接口**：
  - 表：`user_assignments`（已含 `object_type` / `object_id` / `part_id` / `operation_id` / `message` / `is_deleted`）
  - 接口：`POST /api/admin/users/:userId/assignments`（已实现，白名单 `ASSIGNMENT_OBJECT_TYPES`）
  - 取消/删除：`PATCH /api/admin/assignments/:id/cancel`、`PATCH /api/admin/assignments/:id/delete`、`PATCH /api/admin/operations/:operationId/delete`
  - 学员侧读取：`GET /api/user/assignments/today`
- **规则与边界**：
  - **单 P 推送时，卡片展示所属单课的封面**（P 无封面）；**卡片标题 = 单课名-P名**（§4.6）；点击直达并自动播放该 P
  - 一次推送生成一个 `operation_id`，同一操作的多条记录**整体聚合展示**、整体取消
  - 推送记录软删除（`is_deleted`），不物理删除，保证历史可追溯
  - **作业与源内容零引用**：推送时固化标题快照（§4.8）；学员「今日作业」按存活内容实时过滤，源内容被删后条目直接消失；老师端学习记录为纯浏览快照
- **验收标准**：
  1. 四类对象 + 单个 P 均可推送成功
  2. 单 P 推送在学员端显示所属单课封面，**标题为「单课名-P名」**，点击自动播放该 P
  3. 搜索「单课名」可命中其下所有 P；搜索「P 名」可命中并显示拼接名（§4.6）
  4. ≤5 限制生效；同一 operation 聚合展示、可整体取消
  5. 老师推送范围仅限本人班级学生

---

### 5.9 学员首页

**状态**：⚠️ 页面在需返工（含停用态）。

- **目标**：学员登录后的第一屏——看到老师布置的作业与留言
- **入口与权限**：仅学员（页头「首页」）；游客/老师/教导主任无此页
- **页面与交互**（参照 `student-home.html`）：
  - **今日作业**：作业卡片列表，展示推送内容（单 P 用所属单课封面，**标题 = 单课名-P名**，见 §4.6）+ 老师留言
  - 点击卡片按 §5.8 跳转规则进入对应页面
  - **停用态**：页首显示提示条「资源免费开放半年用于自学，如需恢复课程请联系老师。」（逐字，见 §8）
- **数据与接口**：`GET /api/user/assignments/today`（已实现）
- **规则与边界**：停用学员仍可正常浏览视频与下载资料，仅首页/历史作业显示引导文案
- **验收标准**：
  1. 正常态展示作业卡片 + 留言，点击跳转正确
  2. 停用态文案逐字正确，且不影响浏览与下载
  3. 老师/教导主任/游客访问 `/` 不被渲染为学员首页（按角色重定向）

---

### 5.10 我的历史作业

**状态**：❌ 未实现（数据基础已有）。

- **目标**：学员查看历次收到的作业
- **入口与权限**：仅学员（页头「我的历史作业」）
- **页面与交互**（参照 `my-homework.html`）：按 `operation_id` **聚合**展示历次作业（时间、内容、留言）；点击可按 §5.8 进入对应内容；**停用态**显示「老师之前带你学到了这里，如需恢复课程请联系老师。」（逐字，见 §8）
- **数据与接口**：基于 `user_assignments` 按 `operation_id` 聚合；建议新增 `GET /api/user/assignments/history`（或复用 `/api/admin/users/:userId/assignments` 的学员版）
- **规则与边界**：不显示学习进度；软删除的记录不展示（或标注已取消，二选一，建议不展示）
- **验收标准**：
  1. 历次作业按时间倒序聚合展示
  2. 停用态文案逐字正确
  3. 非学员访问被拦截

---

### 5.11 登录与游客模式

**状态**：⚠️ 页面在需返工（含文案口径）。

- **目标**：统一登录入口 + 游客浏览入口
- **页面与交互**（参照 `login.html`，独立水彩明信片风格）：
  - 输入项：**账号 + 密码**（文案不得写「用户名」）；无注册、无找回密码入口
  - 提供「游客浏览」入口，点击进入 `/tracks`
- **数据与接口**：`POST /api/user/login`（学员）、`POST /api/admin/login`（老师/教导主任）；`GET /api/user/me`、`GET /api/admin/me`；`POST /api/{user|admin}/logout`
- **规则与边界**：
  - 登录失败不区分「账号不存在」与「密码错误」（防枚举）
  - **停用账号可正常登录**（见 §2.6）
  - 登录成功按角色跳转（§2.5）
- **验收标准**：
  1. 账号+密码可登录三类身份，跳转正确
  2. 游客入口进入后落在 `/tracks`，且页头无首页/我的历史作业
  3. 停用账号登录后看到对应引导状态

---


## 6. 数据模型（现网真实表）

以下表**已存在于** `backend/src/db.js` 的 `ensureAppSchema()`，开发时按需补列，不要另建重复表。例外：`video_renditions`（清晰度多档，§4.7）**尚未建**，随 M0 新增。

| 表                                     | 用途             | 关键字段                                                                                  |
| ------------------------------------- | -------------- | ------------------------------------------------------------------------------------- |
| `videos`                              | 视频主体（P 的视频载体）  | title, description, video_url, cover_url                                              |
| `admins`                              | 老师 / 教导主任      | username, account, nickname, role, status                                             |
| `users`                               | 学员             | username, account, nickname, class_id, status, is_active                              |
| `teaching_classes`                    | 班级             | name, teacher_id                                                                      |
| `teacher_classes`                     | 老师↔班级（多对多）     | teacher_id, class_id                                                                  |
| `user_assignments`                    | 作业推送记录         | user_id, video_id, object_type, object_id, part_id, message, operation_id, is_deleted |
| `track_points` / `track_collections`  | 曲目 / 曲谱集       | name, cover, description, updated_at                                                  |
| `track_parts`                         | 曲目分 P          | track_id, part_no, video_id, title, duration                                          |
| `collection_tracks`                   | 曲谱集↔曲目         | collection_id, track_id                                                               |
| `knowledge_points` / `knowledge_collections` | 知识点 / 知识点集     | 同 track_points / track_collections                                                   |
| `knowledge_parts`                            | 知识点分 P         | knowledge_point_id, part_no, video_id, title, duration                                |
| `knowledge_collection_items`                 | 知识点集↔知识点       | collection_id, knowledge_point_id                                                     |
| `content_attachments`                 | 内容附件（通用）       | object_type, object_id, file_name, file_url, file_key                                 |
| `video_attachments`                   | 旧视频附件（兼容）      | video_id, file\_*                                                                     |
| `library_resources`                   | 图书馆资料          | title, description, cover, file_name, file_url, file_key                              |
| `library_resource_links`              | 资料↔内容对象关联      | library_resource_id, object_type, object_id                                           |
| `prerequisites`                       | 通用前置关系         | object_type, object_id, prerequisite_type, prerequisite_id（CHECK 禁止自引用）               |
| `video_prerequisites`                 | 旧视频前置（**待退役**） | video_id, prerequisite_video_id                                                       |

待补/待调整：

- `admins` 需支持停用/恢复接口（字段已有，缺 `PATCH /api/admin/admins/:id/status`）
- `library_resources` 表已建，但**后端路由与前端页面均缺**
- `prerequisites` 表已建，但**无对外接口**（详情页读写前置需新增）

---

## 7. 接口清单（已有 / 待补）

**挂载前缀**（`backend/src/server.js`）：`/api/auth`、`/api/admin`、`/api/user`、`/api/tracks`、`/api/knowledge`、`/api/videos`。

| 域                      |  状态 | 说明                         |
| ---------------------- | :-: | -------------------------- |
| `/api/auth/*`          |  ✅  | 兼容登录入口                     |
| `/api/admin/*`         |  ✅  | 账号、班级、学生管理、作业推送、留言（27 个接口） |
| `/api/user/*`          |  ✅  | 学员登录、me、logout、今日作业        |
| `/api/tracks/*`        |  ✅  | 曲目/曲谱集/P/分片直传（17 个接口）      |
| `/api/knowledge/*`     |  ✅  | 知识点区同构（17 个接口）             |
| `/api/videos/*`        |  ➖  | 旧视频管理，随旧页面退役               |
| `/api/library/*`       |  ❌  | **需新建**（§5.3）              |
| `/api/prerequisites` 类 |  ❌  | **需新增**：详情页前置关系读写          |

待补接口建议：

| 方法                    | 路径                                         | 说明                     |
| --------------------- | ------------------------------------------ | ---------------------- |
| GET                   | `/api/prerequisites?objectType=&objectId=` | 读取某对象的前置列表             |
| PUT                   | `/api/prerequisites`                       | 整体覆盖某对象的前置列表（教导主任）     |
| PATCH                 | `/api/admin/admins/:id/status`             | 停用 / 恢复老师与教导主任         |
| GET                   | `/api/user/assignments/history`            | 学员历史作业（按 operation 聚合） |
| GET/POST/PATCH/DELETE | `/api/library/*`                           | 图书馆资料 CRUD + 上传（§5.3）  |

---

## 8. 文案清单（逐字，不得改写）

| 场景             | 文案                               |
| -------------- | -------------------------------- |
| 学员首页 · 停用态     | 资源免费开放半年用于自学，如需恢复课程请联系老师。        |
| 我的历史作业 · 停用态   | 老师之前带你学到了这里，如需恢复课程请联系老师。         |
| 老师学生管理 · 停用态标题 | 此账号已停用                           |
| 老师学生管理 · 停用态正文 | 请联系教导主任获取工作内容。停用期间无法查看学生记录与布置作业。 |
| 老师学生管理 · 暂无带班  | 请联系教导主任获取工作内容                    |
| 搜索 · 分区空态      | XX 中未找到与「关键词」相关的内容               |
| 播放列表 · 正在播放    | 正在播放                             |
| 推送面板 · 搜索框     | 搜索合集 / 单课 / 单 P 名称…              |
| 推送面板 · 留言框     | 作业备注 / 给学生的留言（选填）…               |
| 删除作业 · 原因输入    | 请填写删除原因（如：手滑布置错了）…               |
| 新建单课 · P 名输入   | P 名，如：完整示范 · 原速                  |
| 图书详情 · 类型标注    | 图书馆 · PDF 资料                     |

---

## 9. 非目标（Non-goals）

- 注册、自助改密、找回密码 → 账号由教导主任发放
- 学习进度、已看标记、学习时长统计
- 视频码率自适应（HLS/DASH）/ 播放权限分级（清晰度 = **自动转码三档 + 手动切换**，见 §4.7；转码失败不阻塞原画播放）
- 评论、弹幕、点赞
- 数据看板、报表
- 付费与商业化
- 消息推送（站外通知、短信）
- 旧数据迁移（本轮清空重来）

---

## 10. 验收总表

|  #  | 验收项                                              | 关联           |
| :-: | ------------------------------------------------ | ------------ |
|  1  | 四角色页头与 §2.2 矩阵完全一致；越权入口不渲染且接口拒绝                  | §3.1         |
|  2  | 游客无首页、落 `/tracks`，只能播放与下载                        | §2.3 / §5.11 |
|  3  | 老师零内容权限；写接口对老师返回无权限                              | §2.3 / §5.1  |
|  4  | 学生管理单页双角色：老师班级锁死、教导主任自由；学习记录 + 布置作业同页            | §5.6         |
|  5  | 停用学员/老师均可登录，四处文案逐字一致（§8）                         | §2.6         |
|  6  | 曲目区/知识点区首页两区 + 搜索 + 独立排序 + 翻页全部可用                | §5.1 / §5.2  |
|  7  | 详情页右列三盒齐备；无进度条、无已看标记；P 无封面                       | §3.3 / §4.2  |
|  8  | 单课可属于多个合集；删合集不影响单课；删单课清理 COS 对象                  | §5.1.5       |
|  9  | 前置关系支持四类对象、允许成环、走 `prerequisites`                | §4.3         |
|  10 | 推送支持 4 类对象 + 单个 P；单 P 显示所属单课封面且自动播放              | §5.8         |
|  11 | 图书馆可浏览下载；教导主任可上传删除；详情页关联图书馆接入真实数据                | §5.3         |
|  12 | 分片直传链路未被改写，上传失败可 abort 且无孤儿文件                    | §5.5         |
|  13 | 账号模型为 role/account/nickname/password，登录用 account | §2.4         |
|  14 | 视觉与各设计稿逐块一致（对照 HTML 比对，不是「看起来差不多」）               | §3.4         |
|  15 | 自动转码出 1080p/720p 且失败不阻塞原画；清晰度菜单只列就绪档；默认播 1080p；切换保留进度 | §4.7         |

---


## 11. 开工顺序（与开发计划对应）

按 `docs/开发计划-组件复用与实施.md` §3 执行，PRD 提供每张任务卡的**需求依据**：

|  顺序 | 开发计划任务卡                      | 需求依据                                     |
| :-: | ---------------------------- | ---------------------------------------- |
|  1  | **M0** 清理、初始化与数据地基           | §2.4 账号模型、§4.1 内容模型、§4.4 枚举、§6 数据模型      |
|  2  | **M1** 权限基础设施                | §2.2 权限矩阵、§5.5 上传约束                      |
|  3  | **M2** 设计系统落地（**必须最先做完的基建**） | §3.2 / §3.3 / §3.4                       |
|  4  | **M3** 曲目区（含 UI 返工）          | §5.1                                     |
|  5  | **M4** 知识点区（复用曲目区）           | §5.2                                     |
|  6  | **M5** 作业推送扩展                | §5.8                                     |
|  7  | **M6** 权限页面、停用机制与导航          | §2.2 / §2.6 / §3.1 / §5.6 / §5.7 / §5.11 |
|  8  | **M7** 图书馆                   | §5.3                                     |
|  9  | **M8** 学员首页 + 我的历史作业         | §5.9 / §5.10                             |
|  10 | **M9** 退役页清理与回归              | §10 验收总表                                 |

**纪律**（与开发计划一致，重申）：

1. 一次一张任务卡，自检通过再开下一张
2. 每页改完，**打开对应设计稿 HTML 逐块比对**；标准是「和设计稿长得一样」
3. UI 返工阶段只改视图层，功能问题另开记录
4. 上传相关改动必须在提交信息中单独说明
5. 页头/登录改动后，四个角色各登录一次自检

---

*变更记录：*  
*v0.1 完整版（废弃）；v0.2 精简重写；v0.3 权限重设计（内容权与管理权分离、停用机制、一师多班、学员管理复用）。*  
***v1.0**：面向开发补全——新增 §0 阅读指引与总索引、§3 全局页面规范、§4 内容模型与 object_type 枚举统一、§5 各模块固定六段式（目标/权限/页面/数据接口/规则边界/验收）、§6 现网数据模型、§7 接口清单与缺口、§8 逐字文案、§10 验收总表、§11 与开发计划的开工对应表；修正 v0.3 遗留的三处不一致：设计规范升级为 v2.0（纯白底+焦糖格纹，非纸底）、object_type 统一、图书馆与前置关系接口标记为缺失。*  
***v1.0.1（2026-09-11 晚）**：命名对称化定稿——object_type `track`→`track_point`、`collection`→`track_collection`；数据库表名同步去简写：`tracks`→`track_points`、`collections`→`track_collections`、`collection_tracks`→`track_collection_items`、`kp_collections`→`knowledge_collections`、`kp_parts`→`knowledge_parts`、`kp_collection_items`→`knowledge_collection_items`；COS key 前缀 `kp/`→`knowledge/`。**API 路由（`/api/tracks/*` 等）不变**。前后端代码已同步完成重命名，旧库清空重建。*  
***v1.0.2（2026-09-12）**：①设计稿重命名对齐术语（5 份，见 §0.1 注）并核对全部设计稿细节；②新增 **§4.6 P 命名与展示规范**（跨上下文显示「单课名-P名」，布置作业搜索依赖）；③明确两区「首页 → 合集详情页 → 单课详情页」点击链路并写入验收；④图书馆新增图书详情页（`library-book.html`）；⑤内容创建补入口路由页与表单字段细节；⑥推送面板补类型筛选/搜索框/删除原因等设计稿细节，§8 文案清单同步扩充。*  
***v1.0.3（2026-09-12）**：新增 **§4.7 视频清晰度**——`source`/`1080p`/`720p` 三档枚举、新增 `video_renditions` 表、播放器档位菜单、切换保留进度。*  
***v1.0.4（2026-09-12）**：清晰度方案改为**自动转码**（用户确认）——服务端 ffmpeg 串行队列，source 上传完成后自动转出 1080p/720p（status: ready/processing/failed，失败重试 1 次且**不阻塞原画播放**）；教导主任只需上传一个原画文件（§5.4 同步简化）；播放器**默认播放 1080p**，未就绪时回退原画；§9 仅保留「码率自适应（HLS/DASH）」为非目标；验收 #15 更新；开发计划 M0（表含 status）/ M1（新增转码模块卡）/ M3（验收⑧）同步。*  
***v1.0.5（2026-09-12）**：转码实现改用**腾讯云数据万象（CI）工作流**，不自建 ffmpeg——开通 CI 并绑定桶后，控制台建 1080p/720p 转码模板 + 工作流绑定 `videos/` 路径即自动触发转码；后端只保留回调登记胶水代码（CI 不可用时仅剩原画档，系统照常工作）；开发计划 M1 同步替换。*
***v1.0.6（2026-09-12）**：新增 **§4.8 内容删除与作业快照策略**（用户定稿）——内容一律物理删除（二次确认 + 操作日志，无回收站）；删除时引用直接消失并重算计数，无「已失效」态；作业推送零引用、只存标题快照，学员今日作业按存活内容过滤（全删显示空态），老师端学习记录为纯浏览快照不可点击；全站不统计学习进度/收看数据。§5.6 / §5.8 同步补充。*
